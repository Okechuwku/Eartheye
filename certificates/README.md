# Certificates

Place the profile certificate PDF here as:

- `cybersecurity_certificate.pdf`

This allows the GitHub profile README link to resolve correctly:

- `./certificates/cybersecurity_certificate.pdf`

If you want, you can replace this note with the actual certificate file before publishing the profile repo.